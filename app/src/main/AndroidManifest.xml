<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidbasicscourse">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidBasicsCourse"
        tools:targetApi="31">
        <activity
            android:name=".HomePage"
            android:exported="true"

            />
        <activity
            android:name=".ToDoList"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PracticeActivity"
            android:exported="false" /> <!-- activity - активность(экран) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- intent-filter - настривает как система будет находить и запускать мейнАктивити -->
            <!--
              action.MAIN - главная точка входа в приложение
              category.LAUNCHER - делает активность видимой в списке приложений( в лаунчере),
              отображает икноку на рабочем столе
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>